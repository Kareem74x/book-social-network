spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB

springdoc:
  default-produces-media-type: application/json
  swagger-ui:
    csrf:
      enabled: true

server:
  servlet:
    context-path: /api/v1/